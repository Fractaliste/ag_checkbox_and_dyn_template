import { AsideLoaderDirective } from './aside-loader.directive';

describe('AsideLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
